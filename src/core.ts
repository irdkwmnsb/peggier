import { generateLexer } from '@peggier/generate/lexer';
import { generateParser } from '@peggier/generate/parser';
import { parse } from "@peggier/parser";

export {
  generateLexer,
  generateParser,
  parse
}
